// ─────────────────────────────────────────────────────────────
// Our Bucket List — configuration
// This is the ONLY file you need to edit.
//
// Get both values from: Supabase → your project →
// Project Settings → API
// • "Project URL" → SUPABASE_URL
// • "anon public" key → SUPABASE_ANON_KEY
//
// Note: the anon key is designed to be public — it's safe to
// commit to a public repo. Access control is enforced by the
// database rules in setup.sql, not by hiding this key.
// ─────────────────────────────────────────────────────────────
window.BUCKET_CONFIG = {
    SUPABASE_URL: "https://mmlywxnwdfissfyhgbmu.supabase.co",
    SUPABASE_ANON_KEY: "sb_publishable_XbhyTh15ha9iX8J-HFQ4XA_iIsnmGyz"
};
